==========================================
Welcome to ORT_OS, the new rival of Windows
=========================================

shell%ORT$>echo hello
hello
shelll%ORT$>apt install sudo
intalling sudo..
bla bla bla
sucess_pkg >><< sudo


::::::::::::::inputKey:::::::::::::::::::::::::
while (1) {
        int h = keyboard_getchar();
        if (h == '\n') {
            imp_text("\n");
        } else if (h == '\b') {
            // rudimentary backspace handling: print backspace and space and backspace
            imp_text("\b \b");
        } else {
            imp_char((char)h);
        }
    }

set gfxmode=640x480
set gfxpayload=disable

docker build buildenv -t ort-build
docker run --rm -it -v "$(pwd)":/root/env ort-build


::::::::::::::new commands:::::::::::::::::::::::::::
cmake -S . -B build
cmake --build build
ctest --test-dir build --output-on-failure
./build/storage_path_test
docker build -f buildenv/Dockerfile -t ort-build .
docker run --rm -v "$PWD:/root/env" -w /root/env ort-build \
  sh -lc 'cmake -S . -B build && cmake --build build'

::::::::::::::::::::new commands:::::::::::::::::::::::

docker run --rm -v "$PWD:/root/env" -w /root/env ort-build \
  sh -lc 'cmake -S . -B build-docker && cmake --build build-docker'

OS Features

Core:
- Bootloader
- Kernel
- Memory management
- Process and thread management
- Interrupt handling
- System calls
- Device drivers
- File system
- Command-line shell

User Interface:
- Graphical desktop
- Windows
- Taskbar/panel
- Mouse and keyboard support
- File manager
- Settings
- Notifications
- Login/lock screen
- Clipboard
- Terminal

Networking:
- Ethernet
- Wi-Fi
- TCP/IP
- DNS
- Network settings
- Sockets/API
- Firewall

Security:
- User accounts
- Permissions
- Process isolation
- File permissions
- Secure boot
- Application sandboxing
- Password/PIN authentication
- Security updates

Applications:
- Application/executable format
- Package manager
- App store/repository
- Shared libraries
- Application APIs/SDK
- Configuration system
- System logs
- Crash reporting

Advanced:
- USB support
- Bluetooth
- Audio
- GPU acceleration
- Multicore CPU support
- Power management
- Sleep/hibernate
- Printing
- Camera/microphone support
- Virtualization
- Containers
- Disk encryption
- System recovery
- Accessibility features